% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glover_model.R
\name{get_straight_stream_drawdown_ratio}
\alias{get_straight_stream_drawdown_ratio}
\title{Drawdown near a straight constant-head stream}
\usage{
get_straight_stream_drawdown_ratio(
  df,
  x1 = NULL,
  x2 = NULL,
  y_diff = NULL,
  K = NULL,
  D = NULL,
  V = NULL,
  t = NULL,
  well_diam = NULL
)
}
\arguments{
\item{df}{\code{data.frame} with columns specifying all parameters}

\item{x1}{Perpendicular distance from the pumping well to the stream.}

\item{x2}{Perpendicular distance from the observation well to the stream.}

\item{y_diff}{Distance between the pumping and observation wells parallel to
the stream.}

\item{K}{Saturated hydraulic conductivity}

\item{D}{Depth of aquifer}

\item{V}{Drainable porosity of aquifer}

\item{t}{Time from pumping onset at which to calculate stream depletion
fraction}

\item{well_diam}{Pumping-well diameter. Drawdown does not increase within
one well radius. Defaults to zero.}
}
\value{
A units vector containing the ratio of water-level change to pumping
  rate, with dimensions of time divided by length squared.
}
\description{
Estimate water-level change at an observation location using a pumping well
and its image across a straight, fully penetrating constant-head stream.
}
\details{
This function subtracts the response of an equal-magnitude image injection
well from the pumping-well response. Use [get_aquifer_drawdown_ratio()] when
the stream is represented explicitly by distributed injection wells; using
both approaches would count the stream boundary twice.
}
\examples{
x1 <- units::set_units(1000, "m")
x2 <- units::set_units(500, "m")
y_diff <- units::set_units(250, "m")

get_straight_stream_drawdown_ratio(
  x1 = x1,
  x2 = x2,
  y_diff = y_diff,
  K = units::set_units(1e-5, "m/s"),
  D = units::set_units(50, "m"),
  V = 0.15,
  t = units::set_units(1, "year")
)

}
