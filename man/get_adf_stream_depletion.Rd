% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adf-apportionment.R
\name{get_adf_stream_depletion}
\alias{get_adf_stream_depletion}
\title{Calculate ADF stream depletion}
\usage{
get_adf_stream_depletion(
  pumping_wells,
  pumping_schedules,
  stream_apportionment,
  evaluation_times = NULL
)
}
\arguments{
\item{pumping_wells}{An `sf` pumping-well object accepted by
[`.validate_pumping_wells()`]. Its `pump_id` values must match the
apportionment object and pumping-schedule columns.}

\item{pumping_schedules}{A shared, wide-format pumping schedule accepted by
[`.validate_pumping_schedules()`].}

\item{stream_apportionment}{An `sf` object returned by
[get_stream_reach_apportionment()].}

\item{evaluation_times}{Either `NULL`, a `Date` vector, or a `units` time
vector. When `NULL`, `pumping_schedules$t` is used.}
}
\value{
A tibble with one row per pump, evaluation time, and stream segment.
}
\description{
Apply an ADF stream apportionment to intermittent pumping schedules using
Glover response fractions and superposition.
}
\details{
This is the preferred ADF-specific name. The older
[get_apportioned_stream_depletion()] function remains available for
compatibility and is planned for deprecation.
}
